{
  "name": "komodo-ui-mono-repo",
  "productName": "Komodo UI mono repo",
  "version": "0.13.0",
  "private": true,
  "description": "> TODO: description",
  "scripts": {
    "postinstall": "lerna exec -- npm install --package-lock-only && lerna run prepare",
    "mono-test": "lerna run test",
    "mono-prepare": "lerna run prepare",
    "mono-coverage": "lerna run coverage",
    "update-authors": "./bin/update-authors"
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "cross-env NODE_ENV=development eslint --cache --format=pretty",
      "prettier --ignore-path .eslintignore --single-quote --write",
      "git add"
    ],
    "{*.json,.{babelrc,eslintrc,prettierrc,stylelintrc}}": [
      "prettier --ignore-path .eslintignore --parser json --write",
      "git add"
    ],
    "*.{css,scss}": [
      "stylelint --ignore-path .eslintignore --syntax scss --fix",
      "prettier --ignore-path .eslintignore --single-quote --write",
      "git add"
    ],
    "*.{yml,md}": [
      "prettier --ignore-path .eslintignore --single-quote --write",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/particle4dev/atomicdex.git"
  },
  "author": {
    "name": "Hoang Nam",
    "email": "particle4dev@gmail.com",
    "url": "https://github.com/particle4dev"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/particle4dev/atomicdex/issues"
  },
  "homepage": "https://github.com/particle4dev/atomicdex#readme",
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "jest": "^23.6.0",
    "lerna": "^3.6.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {},
  "engines": {
    "node": ">=10.x <12",
    "npm": ">=6.x",
    "yarn": ">=1.x"
  },
  "browserslist": "electron 1.6"
}
