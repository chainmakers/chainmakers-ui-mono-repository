{
  "packages": ["packages/*"],
  "version": "0.0.0",
  "command": {
    "publish": {
      "ignoreChanges": [
        "*.md",
        "*.txt",
        "test/**",
        "codemods/**",
        "@(!(built-in-modules|built-ins|plugins)).json"
      ]
    }
  }
}
