{
  "lerna": "2.0.0",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "version": "1.0.0",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": [
        "*.md",
        "*.txt",
        "test/**",
        "codemods/**",
        "@(!(built-in-modules|built-ins|plugins)).json"
      ]
    }
  }
}
